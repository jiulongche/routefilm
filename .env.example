# Optional GPT Image 2 generation. RouteFilm automatically reads a local .env file.
# Both values are required. This example is safe to commit; a populated .env is not.
ROUTEFILM_IMAGE_BASE_URL=https://api.openai.com/v1
ROUTEFILM_IMAGE_API_KEY=
